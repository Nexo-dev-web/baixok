{
  "name": "baixok",
  "version": "2.0.0",
  "private": true,
  "description": "Sistema Baixo K - cardapio digital, comanda de mesa, KDS, estoque e telao",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "comentario-engines": "node:sqlite (DatabaseSync, usado em backend/src/db/connection.js) so existe a partir do 22.5. Com o 20 declarado antes, um host que respeita este campo escolhia o 20 e o backend morria na subida com 'Cannot find module node:sqlite'.",
  "engines": {
    "node": ">=22.5"
  },
  "scripts": {
    "dev": "concurrently -n api,web -c cyan,magenta \"npm:dev:api\" \"npm:dev:web\"",
    "dev:api": "npm run dev --workspace backend",
    "dev:web": "npm run dev --workspace frontend",
    "build": "npm run build --workspace frontend",
    "start": "npm run start --workspace backend",
    "migrate": "npm run migrate --workspace backend",
    "seed": "npm run seed --workspace backend",
    "import:legado": "npm run import:legado --workspace backend",
    "test": "npm run test --workspace backend",
    "lint": "eslint ."
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "eslint": "^9.18.0"
  }
}
